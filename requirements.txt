
# run source env/bin/activate in the project directory to activate
# run which pip3 to check if you are looking at the virtual env or global
# run pip3 freeze to get list of installed packages


beautifulsoup4==4.4.1
requests==2.10.0
robobrowser==0.5.3
six==1.10.0
Werkzeug==0.11.10

